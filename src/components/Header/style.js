import styled from 'styled-components';

const ContainerStyle = styled.header`
  display: flex;
  height: 100px;
  
  > img {
    width: 90px;
    margin: auto 85px;
  }
`;

export default ContainerStyle;